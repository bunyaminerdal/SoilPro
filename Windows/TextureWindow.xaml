<Window x:Class="ExDesign.Windows.TextureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExDesign.Windows"
        mc:Ignorable="d"
        Title="TextureWindow" Height="450" Width="800" WindowStyle="ToolWindow" ResizeMode="NoResize" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="120"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="2" HorizontalAlignment="Right">
            <Button  x:Name="select_button" Content="{DynamicResource Select}" HorizontalAlignment="Left"  VerticalAlignment="Top" Width="50" Margin="5" Click="select_button_Click"  />
            <Button  x:Name="cancel_button" Content="{DynamicResource Cancel}" HorizontalAlignment="Left"  VerticalAlignment="Top" Width="50" Margin="5" Click="cancel_button_Click" />
            
        </DockPanel>
        <StackPanel x:Name="TexturePanel" Grid.Row="1"></StackPanel>
    </Grid>
</Window>
